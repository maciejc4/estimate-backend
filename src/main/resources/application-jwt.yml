# JWT Authentication Profile Configuration

auth:
  provider: jwt

# JWT Configuration
app:
  jwt:
    secret: ${JWT_SECRET:defaultSecretKeyForDevelopmentOnlyChangeInProduction123456789}
    expiration-ms: 86400000
  security:
    max-login-attempts: 5
    lockout-duration-minutes: 15
